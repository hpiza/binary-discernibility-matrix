#ifndef PRINTS_H_
#define PRINTS_H_

#include "types.h"

void printBM(bmatrix*, size);
void printBytes(byte*, size);
void printNode(snode*, size printed, size toPrint);
void printStrings(sarray*);
void printTM(tmatrix*, size);

#endif /* PRINTS_H_ */

